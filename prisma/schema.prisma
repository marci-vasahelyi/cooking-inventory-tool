generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
}

model InventoryItem {
  id        String   @id @default(uuid())
  name      String
  quantity  String
  clientId  String
  createdAt DateTime @default(now())

  @@map("inventory_items")
}
